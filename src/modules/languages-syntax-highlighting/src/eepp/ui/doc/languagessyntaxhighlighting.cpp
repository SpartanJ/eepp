#include <eepp/ui/doc/languages/ada.hpp>
#include <eepp/ui/doc/languages/adept.hpp>
#include <eepp/ui/doc/languages/angelscript.hpp>
#include <eepp/ui/doc/languages/awkscript.hpp>
#include <eepp/ui/doc/languages/batchscript.hpp>
#include <eepp/ui/doc/languages/bazel.hpp>
#include <eepp/ui/doc/languages/bend.hpp>
#include <eepp/ui/doc/languages/blade.hpp>
#include <eepp/ui/doc/languages/blueprint.hpp>
#include <eepp/ui/doc/languages/brainfuck.hpp>
#include <eepp/ui/doc/languages/buzz.hpp>
#include <eepp/ui/doc/languages/c2.hpp>
#include <eepp/ui/doc/languages/c3.hpp>
#include <eepp/ui/doc/languages/carbon.hpp>
#include <eepp/ui/doc/languages/clojure.hpp>
#include <eepp/ui/doc/languages/cmake.hpp>
#include <eepp/ui/doc/languages/cmakecache.hpp>
#include <eepp/ui/doc/languages/containerfile.hpp>
#include <eepp/ui/doc/languages/covscript.hpp>
#include <eepp/ui/doc/languages/crystal.hpp>
#include <eepp/ui/doc/languages/csharp.hpp>
#include <eepp/ui/doc/languages/d.hpp>
#include <eepp/ui/doc/languages/dart.hpp>
#include <eepp/ui/doc/languages/difffile.hpp>
#include <eepp/ui/doc/languages/ec.hpp>
#include <eepp/ui/doc/languages/elena.hpp>
#include <eepp/ui/doc/languages/elixir.hpp>
#include <eepp/ui/doc/languages/elm.hpp>
#include <eepp/ui/doc/languages/environmentfile.hpp>
#include <eepp/ui/doc/languages/fantom.hpp>
#include <eepp/ui/doc/languages/fennel.hpp>
#include <eepp/ui/doc/languages/fixscript.hpp>
#include <eepp/ui/doc/languages/flow9.hpp>
#include <eepp/ui/doc/languages/fortran.hpp>
#include <eepp/ui/doc/languages/fstab.hpp>
#include <eepp/ui/doc/languages/gdscript.hpp>
#include <eepp/ui/doc/languages/gleam.hpp>
#include <eepp/ui/doc/languages/glsl.hpp>
#include <eepp/ui/doc/languages/gn.hpp>
#include <eepp/ui/doc/languages/go.hpp>
#include <eepp/ui/doc/languages/graphql.hpp>
#include <eepp/ui/doc/languages/groovy.hpp>
#include <eepp/ui/doc/languages/hare.hpp>
#include <eepp/ui/doc/languages/haskell.hpp>
#include <eepp/ui/doc/languages/haxe.hpp>
#include <eepp/ui/doc/languages/hlsl.hpp>
#include <eepp/ui/doc/languages/htaccess.hpp>
#include <eepp/ui/doc/languages/idl.hpp>
#include <eepp/ui/doc/languages/ignorefile.hpp>
#include <eepp/ui/doc/languages/ispc.hpp>
#include <eepp/ui/doc/languages/jai.hpp>
#include <eepp/ui/doc/languages/janet.hpp>
#include <eepp/ui/doc/languages/java.hpp>
#include <eepp/ui/doc/languages/jsx.hpp>
#include <eepp/ui/doc/languages/jule.hpp>
#include <eepp/ui/doc/languages/julia.hpp>
#include <eepp/ui/doc/languages/kotlin.hpp>
#include <eepp/ui/doc/languages/latex.hpp>
#include <eepp/ui/doc/languages/lbstanza.hpp>
#include <eepp/ui/doc/languages/lisp.hpp>
#include <eepp/ui/doc/languages/lobster.hpp>
#include <eepp/ui/doc/languages/makefile.hpp>
#include <eepp/ui/doc/languages/meson.hpp>
#include <eepp/ui/doc/languages/modula2.hpp>
#include <eepp/ui/doc/languages/modula3.hpp>
#include <eepp/ui/doc/languages/moonscript.hpp>
#include <eepp/ui/doc/languages/msbuildsolution.hpp>
#include <eepp/ui/doc/languages/nelua.hpp>
#include <eepp/ui/doc/languages/nim.hpp>
#include <eepp/ui/doc/languages/nix.hpp>
#include <eepp/ui/doc/languages/objeck.hpp>
#include <eepp/ui/doc/languages/objective-c.hpp>
#include <eepp/ui/doc/languages/ocaml.hpp>
#include <eepp/ui/doc/languages/odin.hpp>
#include <eepp/ui/doc/languages/openscad.hpp>
#include <eepp/ui/doc/languages/pascal.hpp>
#include <eepp/ui/doc/languages/perl.hpp>
#include <eepp/ui/doc/languages/php.hpp>
#include <eepp/ui/doc/languages/pico-8.hpp>
#include <eepp/ui/doc/languages/pkgconfig.hpp>
#include <eepp/ui/doc/languages/po.hpp>
#include <eepp/ui/doc/languages/pony.hpp>
#include <eepp/ui/doc/languages/postgresql.hpp>
#include <eepp/ui/doc/languages/powershell.hpp>
#include <eepp/ui/doc/languages/qbs.hpp>
#include <eepp/ui/doc/languages/qmake.hpp>
#include <eepp/ui/doc/languages/r.hpp>
#include <eepp/ui/doc/languages/racket.hpp>
#include <eepp/ui/doc/languages/rave.hpp>
#include <eepp/ui/doc/languages/rcscript.hpp>
#include <eepp/ui/doc/languages/rescript.hpp>
#include <eepp/ui/doc/languages/ring.hpp>
#include <eepp/ui/doc/languages/rpmspec.hpp>
#include <eepp/ui/doc/languages/ruby.hpp>
#include <eepp/ui/doc/languages/rust.hpp>
#include <eepp/ui/doc/languages/sass.hpp>
#include <eepp/ui/doc/languages/scala.hpp>
#include <eepp/ui/doc/languages/scheme.hpp>
#include <eepp/ui/doc/languages/shellscript.hpp>
#include <eepp/ui/doc/languages/smallbasic.hpp>
#include <eepp/ui/doc/languages/solidity.hpp>
#include <eepp/ui/doc/languages/sql.hpp>
#include <eepp/ui/doc/languages/squirrel.hpp>
#include <eepp/ui/doc/languages/svelte.hpp>
#include <eepp/ui/doc/languages/swift.hpp>
#include <eepp/ui/doc/languages/tcl.hpp>
#include <eepp/ui/doc/languages/teal.hpp>
#include <eepp/ui/doc/languages/toml.hpp>
#include <eepp/ui/doc/languages/tsx.hpp>
#include <eepp/ui/doc/languages/typescript.hpp>
#include <eepp/ui/doc/languages/typst.hpp>
#include <eepp/ui/doc/languages/u.hpp>
#include <eepp/ui/doc/languages/v.hpp>
#include <eepp/ui/doc/languages/v1.hpp>
#include <eepp/ui/doc/languages/vala.hpp>
#include <eepp/ui/doc/languages/vb.hpp>
#include <eepp/ui/doc/languages/verilog.hpp>
#include <eepp/ui/doc/languages/vue.hpp>
#include <eepp/ui/doc/languages/wren.hpp>
#include <eepp/ui/doc/languages/x86assembly.hpp>
#include <eepp/ui/doc/languages/xit.hpp>
#include <eepp/ui/doc/languages/xtend.hpp>
#include <eepp/ui/doc/languages/yaml.hpp>
#include <eepp/ui/doc/languages/yuescript.hpp>
#include <eepp/ui/doc/languages/zephir.hpp>
#include <eepp/ui/doc/languages/zig.hpp>

#include <eepp/ui/doc/languagessyntaxhighlighting.hpp>
#include <eepp/ui/doc/syntaxdefinitionmanager.hpp>

namespace EE { namespace UI { namespace Doc { namespace Language {

void LanguagesSyntaxHighlighting::load() {
	auto sdm = SyntaxDefinitionManager::instance();

	sdm->addPreDefinition( {
		"Ada",
		[]() -> SyntaxDefinition& { return addAda(); },
		{ "%.adb$", "%.ads$", "%.ada$" },
	} );

	sdm->addPreDefinition(
		{ "Adept", []() -> SyntaxDefinition& { return addAdept(); }, { "%.adept$" } } );

	sdm->addPreDefinition( {
		"AngelScript",
		[]() -> SyntaxDefinition& { return addAngelScript(); },
		{ "%.as$", "%.asc$" },
	} );

	sdm->addPreDefinition( {
		"Awk Script",
		[]() -> SyntaxDefinition& { return addAwkScript(); },
		{ "%.awk$" },
	} );

	sdm->addPreDefinition( {
		"Batch Script",
		[]() -> SyntaxDefinition& { return addBatchScript(); },
		{ "%.bat$", "%.cmd$" },
		{},
		"bat",
	} );

	sdm->addPreDefinition( {
		"Bazel",
		[]() -> SyntaxDefinition& { return addBazel(); },
		{ "%.bazel$", "%.bzl$", "BUILD" },
	} );

	sdm->addPreDefinition(
		{ "Bend", []() -> SyntaxDefinition& { return addBend(); }, { "%.bend$" } } );

	sdm->addPreDefinition(
		{ "Blade", []() -> SyntaxDefinition& { return addBlade(); }, { "%.b$" } } );

	sdm->addPreDefinition( {
		"Blueprint",
		[]() -> SyntaxDefinition& { return addBlueprint(); },
		{ "%.blp$" },
	} );

	sdm->addPreDefinition( {
		"Brainfuck",
		[]() -> SyntaxDefinition& { return addBrainfuck(); },
		{ "%.bf$" },
	} );

	sdm->addPreDefinition(
		{ "Buzz", []() -> SyntaxDefinition& { return addBuzz(); }, { "%.buzz$" } } );

	sdm->addPreDefinition( { "C2", []() -> SyntaxDefinition& { return addC2(); }, { "%.c2$" } } );

	sdm->addPreDefinition( { "C3", []() -> SyntaxDefinition& { return addC3(); }, { "%.c3t?$" } } );

	sdm->addPreDefinition( {
		"Carbon",
		[]() -> SyntaxDefinition& { return addCarbon(); },
		{ "%.carbon$" },
	} );

	sdm->addPreDefinition( {
		"Containerfile",
		[]() -> SyntaxDefinition& { return addContainerFile(); },
		{ "^[Cc]ontainerfile$", "^[dD]ockerfile$", "%.[dD]ockerfile$" },
		{},
		"dockerfile",
	} );

	sdm->addPreDefinition( {
		"CovScript",
		[]() -> SyntaxDefinition& { return addCovScript(); },
		{ "%.csc$", "%.csp$", "%.ecs$" },
		{ "^#!.*[ /]covscript", "^#!.*[ /]cs" },

	} );

	sdm->addPreDefinition( {
		"Clojure",
		[]() -> SyntaxDefinition& { return addClojure(); },
		{ "%.clj$", "%.cljs$", "%.clc$", "%.edn$" },
	} );

	sdm->addPreDefinition( {
		"CMake",
		[]() -> SyntaxDefinition& { return addCMake(); },
		{ "%.cmake$", "CMakeLists%.txt$" },
		{ "^cmake_minimum_required.*%c" },

	} );

	sdm->addPreDefinition( { "CMakeCache",
							 []() -> SyntaxDefinition& { return addCMakeCache(); },
							 { "^CMakeCache.txt$" } } );

	sdm->addPreDefinition( {
		"Crystal",
		[]() -> SyntaxDefinition& { return addCrystal(); },
		{ "%.cr$" },
	} );

	sdm->addPreDefinition( {
		"C#",
		[]() -> SyntaxDefinition& { return addCSharp(); },
		{ "%.cs$", "%.csx$" },
		{},
		"csharp",
	} );

	sdm->addPreDefinition( {
		"eC",
		[]() -> SyntaxDefinition& { return addeC(); },
		{ "%.ec$", "%.eh$" },
	} );

	sdm->addPreDefinition( {
		"OpenSCAD",
		[]() -> SyntaxDefinition& { return addOpenSCAD(); },
		{ "%.scad$" },
	} );

	sdm->addPreDefinition( {
		"Ring",
		[]() -> SyntaxDefinition& { return addRing(); },
		{ "%.ring$", "%.rh$", "%.rform$" },
	} );

	sdm->addPreDefinition(
		{ "Tcl", []() -> SyntaxDefinition& { return addTcl(); }, { "%.tcl$" } } );

	sdm->addPreDefinition(
		{ "D", []() -> SyntaxDefinition& { return addD(); }, { "%.d$", "%.di$" } } );

	sdm->addPreDefinition(
		{ "Dart", []() -> SyntaxDefinition& { return addDart(); }, { "%.dart$" } } );

	sdm->addPreDefinition( {
		"Diff File",
		[]() -> SyntaxDefinition& { return addDiff(); },
		{ "%.diff$", "%.patch$" },
		{},
		"diff",
	} );

	sdm->addPreDefinition(
		{ "ELENA", []() -> SyntaxDefinition& { return addElena(); }, { "%.l$" } } );

	sdm->addPreDefinition( {
		"Elixir",
		[]() -> SyntaxDefinition& { return addElixir(); },
		{ "%.ex$", "%.exs$" },
	} );

	sdm->addPreDefinition(
		{ "Elm", []() -> SyntaxDefinition& { return addElm(); }, { "%.elm$" } } );

	sdm->addPreDefinition( {
		"Environment File",
		[]() -> SyntaxDefinition& { return addEnvironmentFile(); },
		{ "%.env$", "%.env.[%w%-%_]*$" },
	} );

	sdm->addPreDefinition( {
		"Fantom",
		[]() -> SyntaxDefinition& { return addFantom(); },
		{ "%.fan$", "%.fanx$" },
	} );

	sdm->addPreDefinition(
		{ "Fennel", []() -> SyntaxDefinition& { return addFennel(); }, { "%.fnl$" } } );

	sdm->addPreDefinition( {
		"FixScript",
		[]() -> SyntaxDefinition& { return addFixScript(); },
		{ "%.fix$" },
	} );

	sdm->addPreDefinition(
		{ "Flow9", []() -> SyntaxDefinition& { return addFlow9(); }, { "%.flow$" } } );

	sdm->addPreDefinition( {
		"Fortran",
		[]() -> SyntaxDefinition& { return addFortran(); },
		{ "%.f$", "%.f90$", "%.f95$" },
	} );

	sdm->addPreDefinition(
		{ "fstab", []() -> SyntaxDefinition& { return addFstab(); }, { "fstab" } } );

	sdm->addPreDefinition( {
		"GDScript",
		[]() -> SyntaxDefinition& { return addGDScript(); },
		{ "%.gd$" },
	} );

	sdm->addPreDefinition(
		{ "Gleam", []() -> SyntaxDefinition& { return addGleam(); }, { "%.gleam$" } } );

	sdm->addPreDefinition( {
		"GLSL",
		[]() -> SyntaxDefinition& { return addGLSL(); },
		{ "%.glsl$", "%.frag$", "%.vert$", "%.fs$", "%.vs$", "%.tesc$", "%.tese$" },
	} );

	sdm->addPreDefinition( { "GN", []() -> SyntaxDefinition& { return addGn(); }, { "%.gn$" } } );

	sdm->addPreDefinition( { "Go", []() -> SyntaxDefinition& { return addGo(); }, { "%.go$" } } );

	sdm->addPreDefinition( {
		"GraphQL",
		[]() -> SyntaxDefinition& { return addGraphQL(); },
		{ "%.graphql$", "%.gql$" },
	} );

	sdm->addPreDefinition( {
		"Groovy",
		[]() -> SyntaxDefinition& { return addGroovy(); },
		{ "%.groovy$", "^Jenkinsfile$" },
	} );

	sdm->addPreDefinition( {
		"Haskell",
		[]() -> SyntaxDefinition& { return addHaskell(); },
		{ "%.hs$" },
	} );

	sdm->addPreDefinition(
		{ "Hare", []() -> SyntaxDefinition& { return addHare(); }, { "%.ha$" } } );

	sdm->addPreDefinition(
		{ "Haxe", []() -> SyntaxDefinition& { return addHaxe(); }, { "%.hx$" } } );

	sdm->addPreDefinition(
		{ "HLSL", []() -> SyntaxDefinition& { return addHLSL(); }, { "%.hlsl$" } } );

	sdm->addPreDefinition( {
		".htaccess",
		[]() -> SyntaxDefinition& { return addHtaccessFile(); },
		{ "^%.htaccess$" },
	} );

	sdm->addPreDefinition( {
		".ignore file",
		[]() -> SyntaxDefinition& { return addIgnoreFile(); },
		{ "%..*ignore$" },
	} );

	sdm->addPreDefinition(
		{ "IDL", []() -> SyntaxDefinition& { return addIdl(); }, { "%.idl$" } } );

	sdm->addPreDefinition( {
		"ISPC",
		[]() -> SyntaxDefinition& { return addISPC(); },
		{ "%.ispc$", "%.isph$", "%.ih$" },
	} );

	sdm->addPreDefinition(
		{ "Jai", []() -> SyntaxDefinition& { return addJai(); }, { "%.jai$" } } );

	sdm->addPreDefinition(
		{ "Janet", []() -> SyntaxDefinition& { return addJanet(); }, { "%.janet$" } } );

	sdm->addPreDefinition( {
		"Java",
		[]() -> SyntaxDefinition& { return addJava(); },
		{ "%.java$", "%.bsh$" },
	} );

	sdm->addPreDefinition(
		{ "Jule", []() -> SyntaxDefinition& { return addJule(); }, { "%.jule$" } } );

	sdm->addPreDefinition(
		{ "Julia", []() -> SyntaxDefinition& { return addJulia(); }, { "%.jl$" } } );

	sdm->addPreDefinition( {
		"JSX",
		[]() -> SyntaxDefinition& { return addJSX(); },
		{ "%.jsx$" },
		{},
		"javascriptreact",
	} );

	sdm->addPreDefinition(
		{ "Kotlin", []() -> SyntaxDefinition& { return addKotlin(); }, { "%.kt$" } } );

	sdm->addPreDefinition( {
		"LaTeX",
		[]() -> SyntaxDefinition& { return addLatex(); },
		{ "%.tex$", "%.sty$", "%.ltx$" },
	} );

	sdm->addPreDefinition( {
		"Lisp",
		[]() -> SyntaxDefinition& { return addLisp(); },
		{ "%.lisp$", "%.cl$", "%.el$" },
	} );

	sdm->addPreDefinition(
		{ "L.B. Stanza", []() -> SyntaxDefinition& { return addLbstanza(); }, { "%.stanza$" } } );

	sdm->addPreDefinition( {
		"Lobster",
		[]() -> SyntaxDefinition& { return addLobster(); },
		{ "%.lobster$" },
	} );

	sdm->addPreDefinition( {
		"Makefile",
		[]() -> SyntaxDefinition& { return addMakefile(); },
		{ "^[Mm]akefile$", "%.mk$", "%.make$", "%.mak$", "^Makefile%.am$", "^Makefile%.in$",
		  "%.ppj$", "%.ppw$", "^BSDmakefile$", "^GNUmakefile$", "^SMakefile$", "%.smk$" },
	} );

	sdm->addPreDefinition( {
		"Meson",
		[]() -> SyntaxDefinition& { return addMeson(); },
		{ "meson%.build$", "^meson_options%.txt$" },
	} );

	sdm->addPreDefinition( {
		"Modula2",
		[]() -> SyntaxDefinition& { return addModula2(); },
		{ "%.def$", "%.mod$" },
	} );

	sdm->addPreDefinition( {
		"Modula3",
		[]() -> SyntaxDefinition& { return addModula3(); },
		{ "%.m3$", "%.i3$", "%.mg$", "%.ig$" },
	} );

	sdm->addPreDefinition( {
		"MoonScript",
		[]() -> SyntaxDefinition& { return addMoonscript(); },
		{ "%.moon$" },
	} );

	sdm->addPreDefinition( {
		"MSBuild Solution",
		[]() -> SyntaxDefinition& { return addMsbuildsolution(); },
		{ "%.sln$" },
	} );

	sdm->addPreDefinition( {
		"Nelua",
		[]() -> SyntaxDefinition& { return addNelua(); },
		{ "%.nelua$" },
		{ "^#!.*[ /]nelua" },

	} );

	sdm->addPreDefinition( {
		"Nim",
		[]() -> SyntaxDefinition& { return addNim(); },
		{ "%.nim$", "%.nims$", "%.nimble$" },
	} );

	sdm->addPreDefinition( {
		"Nix",
		[]() -> SyntaxDefinition& { return addNix(); },
		{ "%.nix$" },
	} );

	sdm->addPreDefinition(
		{ "Objeck", []() -> SyntaxDefinition& { return addObjeck(); }, { "%.obs$" } } );

	sdm->addPreDefinition( {
		"Objective-C",
		[]() -> SyntaxDefinition& { return addObjectiveC(); },
		{ "%.m$" },
	} );

	sdm->addPreDefinition( {
		"OCaml",
		[]() -> SyntaxDefinition& { return addOCaml(); },
		{ "%.ml$", "%.mli$" },
	} );

	sdm->addPreDefinition(
		{ "Odin", []() -> SyntaxDefinition& { return addOdin(); }, { "%.odin$" } } );

	sdm->addPreDefinition( {
		"Pascal",
		[]() -> SyntaxDefinition& { return addPascal(); },
		{ "%.pas$", "%.dpr$", "%.dpk$" },
	} );

	sdm->addPreDefinition( { "pkg-config",
							 []() -> SyntaxDefinition& { return addPkgConfig(); },
							 { "%.pc$", "%.pc%.in$" } } );

	sdm->addPreDefinition( {
		"Perl",
		[]() -> SyntaxDefinition& { return addPerl(); },
		{ "%.pm$", "%.pl$" },
		{ "^#!.*[ /]perl" },

	} );

	sdm->addPreDefinition(
		{ "PICO-8", []() -> SyntaxDefinition& { return addPICO8(); }, { "%.p8$" } } );

	sdm->addPreDefinition( {
		"PHP",
		[]() -> SyntaxDefinition& { return addPHP(); },
		{ "%.php$", "%.php3$", "%.php4$", "%.php5$" },
		{ "^#!.*[ /]php" },

	} );

	sdm->addPreDefinition( {
		"PO",
		[]() -> SyntaxDefinition& { return addPO(); },
		{ "%.po$", "%.pot$" },
	} );

	sdm->addPreDefinition(
		{ "pony", []() -> SyntaxDefinition& { return addPony(); }, { "%.pony$" } } );

	sdm->addPreDefinition( {
		"PostgreSQL",
		[]() -> SyntaxDefinition& { return addPostgreSQL(); },
		{ "%.sql$", "%.psql$" },
	} );

	sdm->addPreDefinition( {
		"PowerShell",
		[]() -> SyntaxDefinition& { return addPowerShell(); },
		{ "%.ps1$", "%.psm1$", "%.psd1$", "%.ps1xml$", "%.pssc$", "%.psrc$", "%.cdxml$" },
	} );

	sdm->addPreDefinition( {
		"Qbs",
		[]() -> SyntaxDefinition& { return addQbs(); },
		{ "%.qbs$" },
	} );

	sdm->addPreDefinition( {
		"QMake",
		[]() -> SyntaxDefinition& { return addQmake(); },
		{ "%.pro$", "%.pri$" },
	} );

	sdm->addPreDefinition( {
		"R",
		[]() -> SyntaxDefinition& { return addR(); },
		{ "%.r$", "%.rds$", "%.rda$", "%.rdata$", "%.R$" },
	} );

	sdm->addPreDefinition(
		{ "Racket", []() -> SyntaxDefinition& { return addRacket(); }, { "%.rkt$" } } );

	sdm->addPreDefinition( { "RC Script",
							 []() -> SyntaxDefinition& { return addRcscript(); },
							 { "%.rc$", "%.rc2$", "%.dlg$" } } );

	sdm->addPreDefinition(
		{ "Rave", []() -> SyntaxDefinition& { return addRave(); }, { "%.rave$" } } );

	sdm->addPreDefinition( {
		"ReScript",
		[]() -> SyntaxDefinition& { return addRescript(); },
		{ "%.res$" },
	} );

	sdm->addPreDefinition(
		{ "RPM Spec", []() -> SyntaxDefinition& { return addRpmspec(); }, { "%.spec$" } } );

	sdm->addPreDefinition( {
		"Ruby",
		[]() -> SyntaxDefinition& { return addRuby(); },
		{ "%.rb$", "%.gemspec$", "%.ruby$" },
		{ "^#!.*[ /]ruby" },

	} );

	sdm->addPreDefinition(
		{ "Rust", []() -> SyntaxDefinition& { return addRust(); }, { "%.rs$" } } );

	sdm->addPreDefinition( {
		"Sass",
		[]() -> SyntaxDefinition& { return addSass(); },
		{ "%.sass$", "%.scss$" },
	} );

	sdm->addPreDefinition( {
		"Scala",
		[]() -> SyntaxDefinition& { return addScala(); },
		{ "%.sc$", "%.scala$" },
	} );

	sdm->addPreDefinition( { "Scheme",
							 []() -> SyntaxDefinition& { return addScheme(); },
							 { "%.scm$", "%.ss$", "%.sch$", "%.sf$" } } );

	sdm->addPreDefinition( { "Shell script",
							 []() -> SyntaxDefinition& { return addShellScript(); },
							 { "%.sh$", "%.bash$", "^%.bashrc$", "^%.bash_profile$", "^%.profile$",
							   "%.zsh$", "%.fish$", "^PKGBUILD$", "%.winlib$", "^APKBUILD$" },
							 { "^#!.*[ /]bash", "^#!.*[ /]sh", "^#!.*[ /]zsh", "^#!.*[ /]fish" },
							 "shellscript",
							 { "bash", "zsh", "sh", "fish" } } );

	sdm->addPreDefinition( {
		"SmallBASIC",
		[]() -> SyntaxDefinition& { return addSmallBASIC(); },
		{ "%.bas$" },
	} );

	sdm->addPreDefinition( {
		"Solidity",
		[]() -> SyntaxDefinition& { return addSolidity(); },
		{ "%.sol$" },
	} );

	sdm->addPreDefinition(
		{ "SQL", []() -> SyntaxDefinition& { return addSQL(); }, { "%.sql$" } } );

	sdm->addPreDefinition( {
		"Squirrel",
		[]() -> SyntaxDefinition& { return addSquirrel(); },
		{ "%.nut$" },
	} );

	sdm->addPreDefinition( {
		"Svelte",
		[]() -> SyntaxDefinition& { return addSvelte(); },
		{ "%.svelte$" },
	} );

	sdm->addPreDefinition(
		{ "Swift", []() -> SyntaxDefinition& { return addSwift(); }, { "%.swift$" } } );

	sdm->addPreDefinition( {
		"Teal",
		[]() -> SyntaxDefinition& { return addTeal(); },
		{ "%.tl$", "%.d%.tl$" },
		{ "^#!.*[ /]tl" },

	} );

	sdm->addPreDefinition(
		{ "TOML", []() -> SyntaxDefinition& { return addToml(); }, { "%.toml$" } } );

	sdm->addPreDefinition( { "TypeScript",
							 []() -> SyntaxDefinition& { return addTypeScript(); },
							 { "%.ts$", "%.d%.ts$" },
							 {},

							 { "ts" } } );

	sdm->addPreDefinition( { "TSX",
							 []() -> SyntaxDefinition& { return addTSX(); },
							 { "%.tsx$" },
							 {},
							 "typescriptreact" } );

	sdm->addPreDefinition(
		{ "Typst", []() -> SyntaxDefinition& { return addTypst(); }, { "%.typ$" } } );

	sdm->addPreDefinition( {
		"Ü",
		[]() -> SyntaxDefinition& { return addU(); },
		{ "%.u$", "%.uh$" },
		{},
		"ü",
	} );

	sdm->addPreDefinition(
		{ "V", []() -> SyntaxDefinition& { return addV(); }, { "%.v$", "%.vsh$" } } );

	sdm->addPreDefinition( {
		"V1",
		[]() -> SyntaxDefinition& { return addV1(); },
		{ "%.v1$" },
		{ "^#!.*[ /]v1" },

	} );

	sdm->addPreDefinition( {
		"Vala",
		[]() -> SyntaxDefinition& { return addVala(); },
		{ "%.vala$", "%.genie$" },
	} );

	sdm->addPreDefinition( {
		"Verilog",
		[]() -> SyntaxDefinition& { return addVerilog(); },
		{ "%.V$", "%.vl$", "%.vh$", "%.v$" },
	} );

	sdm->addPreDefinition( {
		"Visual Basic",
		[]() -> SyntaxDefinition& { return addVisualBasic(); },
		{ "%.bas$", "%.cls$", "%.ctl$", "%.dob$", "%.dsm$", "%.dsr$", "%.frm$", "%.pag$", "%.vb$",
		  "%.vba$", "%.vbs$" },
	} );

	sdm->addPreDefinition(
		{ "Vue", []() -> SyntaxDefinition& { return addVue(); }, { "%.vue?$" } } );

	sdm->addPreDefinition(
		{ "Wren", []() -> SyntaxDefinition& { return addWren(); }, { "%.wren$" } } );

	sdm->addPreDefinition( {
		"x86 Assembly",
		[]() -> SyntaxDefinition& { return addX86Assembly(); },
		{ "%.asm$", "%.[sS]$" },
	} );

	sdm->addPreDefinition(
		{ "[x]it!", []() -> SyntaxDefinition& { return addXit(); }, { "%.xit$" } } );

	sdm->addPreDefinition(
		{ "Xtend", []() -> SyntaxDefinition& { return addXtend(); }, { "%.xtend$" } } );

	sdm->addPreDefinition( {
		"YAML",
		[]() -> SyntaxDefinition& { return addYAML(); },
		{ "%.yml$", "%.yaml$" },
		{ "^%%YAML %d+%.%d+" },

	} );

	sdm->addPreDefinition( {
		"YueScript",
		[]() -> SyntaxDefinition& { return addYueScript(); },
		{ "%.yue$" },
	} );

	sdm->addPreDefinition(
		{ "Zephir", []() -> SyntaxDefinition& { return addZephir(); }, { "%.zep$" } } );

	sdm->addPreDefinition(
		{ "Zig", []() -> SyntaxDefinition& { return addZig(); }, { "%.zig$" } } );
}

}}}} // namespace EE::UI::Doc::Language
